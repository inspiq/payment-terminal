import styled from 'styled-components'

const Title = styled.h1`
  font-weight: 800;
  text-align: center;
  font-size: 22px;
  line-height: 36px;
  margin-bottom: 40px;

  > span {
    color: rgb(255, 130, 0);
  }
`

export default Title;
